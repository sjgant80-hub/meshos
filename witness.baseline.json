[
  {
    "mutation": "< → <=",
    "snippet": "for (let t = 16; t < 64; t++) {",
    "reason": "Genuinely equivalent: the mutated bound runs one extra schedule iteration that writes w[64], out of bounds on the fixed Uint32Array(64) — typed-array OOB writes are silently discarded, the loop body has no other side effect, and nothing reads past w[63]. Behaviour is bit-identical; the FIPS vectors pass either way."
  },
  {
    "mutation": "|| → &&",
    "snippet": "if (!isObj(item) || !isStr(item.hash) || !isStr(item.prevHash) || !isObj(item.entry)) retu",
    "reason": "Genuinely equivalent (the middle || variant): it differs only on chains where exactly ONE of item.hash / item.prevHash is a non-string, and both cases are shadowed by the strict recomputation — a non-string hash can never strictly equal the recomputed primitive-string h.hash (caught at the same index), and a non-string prevHash can never strictly equal expectedPrev, which is GENESIS or a hash the previous link already proved equal to a primitive string. The killable forgeries through the outer clauses (an array link with honest fields; a string entry with a matching hash) are killed by the suite."
  }
]
